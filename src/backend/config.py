# Backend Configuration
# 后端配置文件

# WebSocket服务端口（可通过环境变量 WS_PORT 覆盖）
PORT = 30000

# 主机地址（可通过环境变量 WS_HOST 覆盖）
HOST = "0.0.0.0"

# 工作空间路径（可选，默认为 ./workspace）
WORKSPACE_PATH = None

# 日志级别
LOG_LEVEL = "INFO"
